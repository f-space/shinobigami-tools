{
  "name": "shinobigami-tools",
  "version": "1.0.1",
  "description": "",
  "author": "F_",
  "license": "MIT",
  "scripts": {
    "start": "http-server ./public --port 8080",
    "build": "spago bundle-app --to ./public/index.js",
    "build:module": "spago build",
    "test": "spago test --main Test.Main"
  },
  "devDependencies": {
    "http-server": "^0.12.0",
    "purescript": "^0.13.5",
    "spago": "^0.12.1"
  }
}
